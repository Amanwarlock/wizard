### Sass Tutorial

```sh
Complile Sass via terminal : sass input.scss output.css

Watch for changes : sass --watch input.scss output.css

Compile entire folder : sass --watch app/sass:public/stylesheet

```